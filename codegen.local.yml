overwrite: true
schema: 'http://localhost:8000/graphql/'
documents: 'src/**/*.graphql'
generates:
  src/gerated/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      avoidOptionals:
        field: true
        # object: true
      maybeValue: T
